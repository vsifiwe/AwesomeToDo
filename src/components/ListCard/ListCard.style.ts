import { StyleSheet } from "react-native";
import Dimensions from "../../styles/Dimensions";

export default StyleSheet.create({
	container: {
		height: Dimensions.FULL_HEIGHT * 0.4,
		width: Dimensions.FULL_WIDTH * 0.3,
		padding: Dimensions.PADDING,
	},
});
